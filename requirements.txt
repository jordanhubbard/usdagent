fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
httpx>=0.27.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
# OpenUSD Python bindings — install separately (not on PyPI for all platforms).
# Option 1 (Linux/macOS x86_64):  pip install usd-core>=24.3
# Option 2 (any platform):        build from source — https://github.com/PixarAnimationStudios/OpenUSD
# The service runs without pxr using a string-template fallback.
# usd-core>=24.3
# Google Drive integration
google-auth>=2.28.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.124.0
# JWT auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9
jinja2>=3.1.0
